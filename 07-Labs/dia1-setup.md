# Día 1 - Setup del entorno

## IP de mi VM
- IP: 10.0.2.15

## Problemas que tuve
- Al ejecutar `wsl --install` la primera vez, no descargó ninguna distro. Lo resolví con `wsl --install -d Ubuntu`.

## Qué aprendí
- WSL2 usa su propio hipervisor, no depende de Hyper-V.
- VirtualBox necesita virtualización activa en BIOS (Intel VT-x).
- Conceptos básicos de git y git hub (git add, git commit, git push).